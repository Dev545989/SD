name: DKSA Monitor

on:
  workflow_run:
    workflows: ["DKSA"]
    types:
      - completed
  workflow_dispatch:

jobs:
  build_stats:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install boto3 openpyxl pyyaml python-dotenv pandas

      # - name: Upload site.yml
      #   env:
      #     CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
      #     CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
      #     CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
      #     CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
      #   run: |
      #       python << 'EOF'
      #       import boto3
      #       import os
      #       client = boto3.client('s3',
      #                   endpoint_url=os.environ['CF_R2_ENDPOINT_URL'],
      #                   aws_access_key_id=os.environ['CF_R2_ACCESS_KEY_ID'],
      #                   aws_secret_access_key=os.environ['CF_R2_SECRET_ACCESS_KEY'],
      #                   region_name='auto'
      #               )
      #       local_file = "monitor/site.yml" 
      #       object_key = "monitor-sites/DKSA/site.yml"
      #       client.upload_file(local_file, os.environ['CF_R2_BUCKET_NAME'], object_key)
      #       print(f"Uploaded to {os.environ['CF_R2_BUCKET_NAME']}/{object_key}")
      #       EOF

      # - name: Upload websites-config.yml
      #   env:
      #     CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
      #     CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
      #     CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
      #     CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
      #   run: python monitor/build_monitor_data.py --prefix DKSA --upload-config

      - name: Diagnose R2 paths
        env:
          CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
          CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
          CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
          CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
        run: python monitor/diagnose_r2_paths.py --prefix DKSA --days 5

      # - name: Diagnose excel files
      #   env:
      #     CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
      #     CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
      #     CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
      #     CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
      #   run: python monitor/diagnose_excel_files.py --prefix DKSA --category Vehicles --date 2026-08-02
      
      
      # - name: Build stats (test mode)
      #   env:
      #     CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
      #     CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
      #     CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
      #     CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
      #   run: python monitor/build_monitor_data.py --prefix DKSA --build-stats --days 3 --no-save --output-stats test_stats.yml

      # - name: Show test stats
      #   run: cat test_stats.yml


      # - name: Upload monitor_stats.yml
      #   env:
      #     CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
      #     CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
      #     CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
      #     CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
      #   run: python monitor/build_monitor_data.py --prefix DKSA --build-stats --days 3

      # - name: Print monitor_stats.yml
      #   env:
      #     CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
      #     CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
      #     CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
      #     CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
      #   run: |
      #     python << 'EOF'
      #     import boto3
      #     import os
      #     import yaml

      #     client = boto3.client('s3',
      #         endpoint_url=os.environ['CF_R2_ENDPOINT_URL'],
      #         aws_access_key_id=os.environ['CF_R2_ACCESS_KEY_ID'],
      #         aws_secret_access_key=os.environ['CF_R2_SECRET_ACCESS_KEY'],
      #         region_name='auto'
      #     )
      #     bucket = os.environ['CF_R2_BUCKET_NAME']
      #     key = "DKSA/monitor/monitor_stats.yml"

      #     try:
      #         resp = client.get_object(Bucket=bucket, Key=key)
      #         content = resp['Body'].read().decode('utf-8')
      #         data = yaml.safe_load(content)
              
      #         print("=" * 60)
      #         print("📊 MONITOR_STATS.YML")
      #         print("=" * 60)
      #         print(f"Total scrapers: {len(data)}")
      #         print("-" * 60)
              
      #         for scraper_name, stats in data.items():
      #             observed_dates = stats.get('observed_dates', [])
      #             sheets = stats.get('sheets', {})
      #             file_size = stats.get('file_size_kb', {})
                  
      #             print(f"\n🔹 {scraper_name}")
      #             print(f"   Observed dates: {len(observed_dates)} days")
      #             if observed_dates:
      #                 print(f"   First: {observed_dates[0]}")
      #                 print(f"   Last:  {observed_dates[-1]}")
      #             print(f"   Sheets: {len(sheets)}")
      #             if file_size:
      #                 print(f"   File size (KB): min={file_size.get('min')}, max={file_size.get('max')}, last={file_size.get('last')}")
                  
      #             for sheet_name, sheet_data in list(sheets.items())[:3]:
      #                 row_count = sheet_data.get('row_count', {})
      #                 cols = sheet_data.get('columns', [])
      #                 print(f"      - {sheet_name}: {row_count.get('last', 0)} rows, {len(cols)} columns")
      #             if len(sheets) > 3:
      #                 print(f"      ... and {len(sheets) - 3} more sheets")
              
      #         print("\n" + "=" * 60)
              
      #     except Exception as e:
      #         print(f"❌ Error reading monitor_stats.yml: {e}")
      #     EOF

      # - name: Upload report.json
      #   env:
      #     CF_R2_ACCESS_KEY_ID: ${{ secrets.CF_R2_ACCESS_KEY_ID }}
      #     CF_R2_SECRET_ACCESS_KEY: ${{ secrets.CF_R2_SECRET_ACCESS_KEY }}
      #     CF_R2_ENDPOINT_URL: ${{ secrets.CF_R2_ENDPOINT_URL }}
      #     CF_R2_BUCKET_NAME: ${{ secrets.CF_R2_BUCKET_NAME }}
      #   run: |
      #     DATE_PREFIX="2026-08-02"
      #     echo "Generating report for: $DATE_PREFIX"
      #     python monitor/build_monitor_data.py --prefix DKSA --report-date "$DATE_PREFIX"